name: Build and Publish NuGet

on:
  push:
    tags:
      - 'v*'
    paths-ignore:
      - '**.md'        
      - 'LICENSE'      
      - '.gitignore'    
      - 'docs/**'     
      - 'icon.png'      
  workflow_dispatch:

jobs:
  # ===================================================
  # The Matrix Build
  # ===================================================
  build-rust:
    name: Build Rust (${{ matrix.os }} - ${{ matrix.rid }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # --- Linux x64 glibc ---
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            rid: linux-x64
            artifact_name: libnative_shim.so
            use_cross: false
            container_image: quay.io/pypa/manylinux_2_28_x86_64
            rustflags: "-C target-cpu=x86-64-v3"
            container: quay.io/pypa/manylinux_2_28_x86_64

          # --- Linux x64 Musl ---
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            rid: linux-musl-x64
            artifact_name: libnative_shim.so
            use_cross: true
            rustflags: "-C target-feature=-crt-static -C target-cpu=x86-64-v3"
            container: null
            
          # --- Linux ARM64 glibc---
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            rid: linux-arm64
            artifact_name: libnative_shim.so
            use_cross: true
            container: null
            
          # --- Linux ARM64 Musl ---
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            rid: linux-musl-arm64
            artifact_name: libnative_shim.so
            use_cross: true
            rustflags: "-C target-feature=-crt-static"
            container: null
          
          # Windows x64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            rid: win-x64
            artifact_name: native_shim.dll
            use_cross: false
            rustflags: "-C target-cpu=x86-64-v3"
            container: null

          # MacOS ARM64 (Apple Silicon) - 
          - os: macos-latest
            target: aarch64-apple-darwin
            rid: osx-arm64
            artifact_name: libnative_shim.dylib
            use_cross: false
            container: null

    container: ${{ matrix.container }}

    steps:
      - uses: actions/checkout@v4

      - name: Add Swap Space to prevent OOM
        if: runner.os == 'Linux' && matrix.container == null
        uses: pierotofy/set-swap-space@master
        with:
          swap-size-gb: 10

      - name: Remove Local Config (Linux)
        if: runner.os == 'Linux'
        run: rm -rf native_shim/.cargo

      # Install Rust
      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-04-01 
          targets: ${{ matrix.target }}

      # Install Cross
      - name: Install cross
        if: matrix.use_cross == true
        run: cargo install cross --git https://github.com/cross-rs/cross

      - uses: swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}-${{ matrix.rid }}
          workspaces: ./native_shim

      - name: Pull Container Image
        if: matrix.container_image
        run: docker pull ${{ matrix.container_image }}

      # Compile Rust (Release mode)
      - name: Build Native Shim
        shell: bash
        env:
          RUSTFLAGS: ${{ matrix.rustflags }}
        run: |
          if [[ -n "${{ matrix.container_image }}" ]]; then
            echo "Building for ${{ matrix.target }} inside ${{ matrix.container_image }}..."
            echo "RUSTFLAGS: $RUSTFLAGS"
            docker run --rm \
              -v "${{ github.workspace }}:/workspace" \
              -v "$HOME/.rustup:/root/.rustup" \
              -v "$HOME/.cargo:/root/.cargo" \
              -w /workspace/native_shim \
              -e RUSTFLAGS="$RUSTFLAGS" \
              -e RUSTUP_HOME=/root/.rustup \
              -e CARGO_HOME=/root/.cargo \
              ${{ matrix.container_image }} \
              bash -c 'export PATH="/root/.cargo/bin:$PATH" && cargo build --release --target ${{ matrix.target }}'
          else
            CARGO_CMD="cargo"
            if [[ "${{ matrix.use_cross }}" == "true" ]]; then
              CARGO_CMD="cross"
            fi
            echo "Building for ${{ matrix.target }} using $CARGO_CMD..."
            echo "RUSTFLAGS: $RUSTFLAGS"
            cd native_shim
            $CARGO_CMD build --release --target ${{ matrix.target }}
          fi
          
          echo "=== Environmental Inspection ==="
          echo "Runner OS: $RUNNER_OS"

          if [ -f /etc/os-release ]; then
            grep "PRETTY_NAME" /etc/os-release
          fi

          ldd --version 2>/dev/null | head -n 1 || echo "ldd not available (likely musl environment)"
          echo "================================"

          echo "Building for ${{ matrix.target }} using $CARGO_CMD..."
          echo "RUSTFLAGS: $RUSTFLAGS"

          cd native_shim
          $CARGO_CMD build --release --target ${{ matrix.target }}

      - name: Prepare Artifact
        shell: bash
        working-directory: ./native_shim
        run: |
          cp target/${{ matrix.target }}/release/${{ matrix.artifact_name }} .
          echo "Moved artifact to root: ${{ matrix.artifact_name }}"

      # Upload Artifact to Git Actions
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: native-lib-${{ matrix.rid }}
          path: native_shim/${{ matrix.artifact_name }}
          if-no-files-found: error

  # ===================================================
  # Pack .NET NuGet (The Packaging)
  # ===================================================
  package-dotnet:
    needs: build-rust
    runs-on: ubuntu-latest
    name: Pack NuGet Packages
    
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Create Libs Directory
        run: |
          mkdir -p libs/linux-x64
          mkdir -p libs/linux-musl-x64
          mkdir -p libs/linux-arm64
          mkdir -p libs/linux-musl-arm64
          mkdir -p libs/win-x64
          mkdir -p libs/osx-arm64

      - uses: actions/download-artifact@v4
        with: { name: native-lib-linux-x64, path: libs/linux-x64 }
      - uses: actions/download-artifact@v4
        with: { name: native-lib-linux-musl-x64, path: libs/linux-musl-x64 }
      - uses: actions/download-artifact@v4
        with: { name: native-lib-linux-arm64, path: libs/linux-arm64 }
      - uses: actions/download-artifact@v4
        with: { name: native-lib-linux-musl-arm64, path: libs/linux-musl-arm64 }
      - uses: actions/download-artifact@v4
        with: { name: native-lib-win-x64, path: libs/win-x64 }
      - uses: actions/download-artifact@v4
        with: { name: native-lib-osx-arm64, path: libs/osx-arm64 }

      - name: Inspect Directory Structure
        run: ls -R libs/

      - name: Pack Native NuGets
        run: |
          dotnet pack NativePackages/Polars.NET.Native.linux-x64.csproj -c Release -o nupkgs
          dotnet pack NativePackages/Polars.NET.Native.linux-musl-x64.csproj -c Release -o nupkgs
          dotnet pack NativePackages/Polars.NET.Native.linux-arm64.csproj -c Release -o nupkgs
          dotnet pack NativePackages/Polars.NET.Native.linux-musl-arm64.csproj -c Release -o nupkgs
          dotnet pack NativePackages/Polars.NET.Native.win-x64.csproj -c Release -o nupkgs
          dotnet pack NativePackages/Polars.NET.Native.osx-arm64.csproj -c Release -o nupkgs

      - name: Pack Managed Core and APIs
        run: |
          dotnet pack Polars.NET.Core/Polars.NET.Core.csproj -c Release -p:BuildRustNative=false -o nupkgs
          dotnet pack Polars.CSharp/Polars.CSharp.csproj -c Release -p:BuildRustNative=false -o nupkgs
          dotnet pack Polars.FSharp/Polars.FSharp.fsproj -c Release -p:BuildRustNative=false -o nupkgs
          dotnet pack Polars.NET.Linq/Polars.NET.Linq.csproj -c Release -p:BuildRustNative=false -o nupkgs
          dotnet pack Polars.NET.ML/Polars.NET.ML.csproj -c Release -p:BuildRustNative=false -o nupkgs

      - name: Upload NuGet Packages
        uses: actions/upload-artifact@v4
        with:
          name: polars-net-nugets
          path: nupkgs
  # ===================================================
  # Smoke Test
  # ===================================================
  smoke-test:
    needs: package-dotnet 
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false 
      matrix:
        include:
          - os: ubuntu-latest
            native_rid: linux-x64
          - os: ubuntu-24.04-arm
            native_rid: linux-arm64
          - os: windows-latest
            native_rid: win-x64
          - os: macos-14
            native_rid: osx-arm64

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - uses: actions/download-artifact@v4
        with:
          name: polars-net-nugets
          path: local_feed

      - name: Run Smoke Test
        shell: bash
        run: |
          VERSION=$(sed -n 's/.*<Version>\([^<]*\)<\/Version>.*/\1/p' Directory.Build.props 2>/dev/null | head -n 1 | tr -d '\r')
          if [ -z "$VERSION" ]; then
            VERSION=$(sed -n 's/.*<Version>\([^<]*\)<\/Version>.*/\1/p' Polars.NET.Core/Polars.NET.Core.csproj | head -n 1 | tr -d '\r')
          fi

          if [ -z "$VERSION" ]; then
            echo "Failed to extract version!"
            exit 1
          fi

          echo "Detected Version: '$VERSION'"

          dotnet new console -n SmokeTest
          cd SmokeTest
          
          dotnet new nugetconfig
          dotnet nuget add source ../local_feed --name LocalFeed
          
          dotnet add package Polars.NET --version "$VERSION"
          dotnet add package Polars.NET.Native.${{ matrix.native_rid }} --version "$VERSION"
          
          cat <<EOF > Program.cs
          using System;
          using Polars.CSharp; 
          using static Polars.CSharp.Polars;

          class Program {
              static void Main() {
                  Console.WriteLine("Starting Polars.NET Smoke Test...");
                  try {
                      var df = DataFrame.FromColumns(new {
                          Id = new[] { 1, 2, 3 },
                          Val = new[] { 10.0, 20.0, 30.0 }
                      });
                      Console.WriteLine($"[Success] DataFrame Created: {df.Height} rows");
                      Console.WriteLine(" Smoke Test Passed!");
                  } catch (Exception ex) {
                      Console.Error.WriteLine("Error: " + ex.ToString());
                      Environment.Exit(1);
                  }
              }
          }
          EOF
          
          dotnet run